export default defineEventHandler(async (event) => {
    const { userId } = event.context;
    // console.log(userId);
    // if(!userId){
    //     throw createError({
    //         statusCode: 401,
    //         message: '未登录',
    //     })
    // }
})
